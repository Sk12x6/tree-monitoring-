/**
 * secrets.h.template
 *
 * HOW TO USE:
 *   1. Copy this file and rename the copy to  secrets.h
 *   2. Fill in your real values below
 *   3. secrets.h is listed in .gitignore — it will NEVER be uploaded to GitHub
 *
 * WARNING: Never commit secrets.h or paste real credentials into ArborGuard.ino
 */

#pragma once

// ── WiFi ──────────────────────────────────────────────────────
#define WIFI_SSID  "YOUR_WIFI_NETWORK_NAME"
#define WIFI_PASS  "YOUR_WIFI_PASSWORD"

// ── Telegram Bot ───────────────────────────────────────────────
// Get BOT_TOKEN from @BotFather on Telegram (/newbot)
#define BOT_TOKEN  "your bot token"

// Get CHAT_ID by messaging @userinfobot on Telegram
#define CHAT_ID    "your chat id"
